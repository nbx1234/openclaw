spring:
  datasource:
    url: jdbc:mysql://47.94.172.234:3306/openclaw?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: mysql_xRXCDS
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  redis:
    host: 47.94.172.234
    port: 6379
    password: 123456
    database: 0
    timeout: 2000
    jedis:
      pool:
        max-active: 8
        max-idle: 8
        min-idle: 0
        max-wait: -1ms

  jpa:
    hibernate:
      ddl-auto: none

mybatis-plus:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.openclaw.entity
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    root: INFO
    com.openclaw: DEBUG
    org.springframework.web: DEBUG